<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Details</title>
</head>
<body>

    <header>
        <div>
            <a href="/">Home</a>
            <a href="/all">Maybelline Products</a>
            <a href="/mycards">Mycard</a>
        </div>
    </header>


    <div>
            <img src="<%= makeup.image %> " alt="makeup">
            <h4> <%= makeup.name %> </h4>
            <p> <%= makeup.price %> </p>
            <p> <%= makeup.description %> </p>
        </div>
        <button id="updateButton"> Update Card</button>
        <form action="/delete/<%= makeup.id %>?_method=delete " method="post">
            <button>Delete Card</button>
            </form>
        <form id="updateForm" action="/update/<%= makeup.id %>?_method=put " method="post">
            <input type="text" name="name" value="<%= makeup.name %> ">
            <input type="text" name="price" value="<%= makeup.price %> ">
            <input type="text" name="image" value="<%= makeup.image %> ">
            <input type="text" name="description" value="<%= makeup.description %> ">
        <button>Update</button>
    </form>
    <script src="https://code.jquery.com/jquery-3.6.0.slim.min.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>